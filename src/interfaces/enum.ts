export enum TripStatus {
    ASSIGNED = "ASSIGNED",
    VENDOR_AT = "VENDOR_AT",
    PICKED = "PICKED",
    DELIVERED = "DELIVERED"
}

export enum DeliveryReportStatus {
    DELAY = "DELAY",
    CHECKED = "CHECKED",
    FOLLOW_UP = "FOLLOW_UP",
}
